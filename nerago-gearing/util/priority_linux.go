package util

func CurrentProcessLowerPriority() {
	// pid := strconv.Itoa(os.Getpid())
	// cmd := exec.Command(`/usr/bin/renice -n 10 -p ` + pid)
	// if err := cmd.Run(); err != nil {
	// 	panic(err)
	// }
}
